# Analysis handoff

Persist the source digest, parser, schema, row count, missingness, units,
duplicates, transformations, and output digest. A profile is descriptive; it
does not justify causal or predictive claims by itself.
