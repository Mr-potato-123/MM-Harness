# PDF evidence record

For each extracted observation retain:

`artifact_sha256`, `logical_name`, `page_or_region`, `parser`, `extraction_mode`
(`text`, `image`, `table`, or `unknown`), `quoted_or_structured_value`, and a
short `verification_status`. Never put an extraction guess in the observed
facts field.

