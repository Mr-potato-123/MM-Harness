# Figure contract

Store the image with:

- source artifact digests and transformation ID
- chart type, axes/units, aggregation and uncertainty method
- deterministic style/seed and dimensions
- caption and accessibility description
- a check that every plotted value is traceable to the source data

